import React from 'react';
import MyMap from './map/mapcontainer';

const Map = ({userId}) => {
    // if (!userId) {
    //     return <div></div>;
    // } else {
        return (
            <MyMap/>
        );
    // }
};

export default Map;